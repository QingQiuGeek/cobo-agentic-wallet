# x402 服务发现 — 实现方案

## 问题分析

当前 ServiceList 只是一个侧边栏卡片，空间有限（~256px 宽），无法展示：
- 分页列表（默认 20 条/页）
- 搜索筛选
- 服务详情

## 方案设计

### 架构

```
┌──────────────────────────────────────────────────────────────┐
│  左侧边栏（256px）          │  右侧面板（可调宽度）           │
│                              │                                │
│  ┌──────────────────────┐   │  ┌──────────────────────────┐  │
│  │ 💰 Agent Wallet      │   │  │ [APIs] [Tx Ledger] [Logs]│  │
│  └──────────────────────┘   │  ├──────────────────────────┤  │
│  ┌──────────────────────┐   │  │ 🔍 搜索: [__________]    │  │
│  │ 🆔 ERC-8004 Registry │   │  │                          │  │
│  └──────────────────────┘   │  │ ┌──────────────────────┐ │  │
│  ┌──────────────────────┐   │  │ │ ETH Chain Analysis   │ │  │
│  │ 💧 CoBo Faucet       │   │  │ │ 0.0001 ETH           │ │  │
│  └──────────────────────┘   │  │ │ by BlockIntelligence │ │  │
│                              │  │ └──────────────────────┘ │  │
│                              │  │ ┌──────────────────────┐ │  │
│                              │  │ │ Market Prediction    │ │  │
│                              │  │ │ 0.0005 ETH           │ │  │
│                              │  │ └──────────────────────┘ │  │
│                              │  │                          │  │
│                              │  │ [< 1 2 3 ... 10 >]      │  │
│                              │  └──────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
```

**左侧边栏不展示 Available APIs，服务列表全部在右侧面板 APIs Tab 中。**

### 右侧面板 Tab 扩展

在现有的 `Tx Ledger` 和 `Logs` 两个 Tab 基础上，新增 `APIs` Tab：

| Tab | 内容 | 数据源 |
|---|---|---|
| Tx Ledger | 交易记录 | CAW `listUserTransactions` |
| Logs | 审计日志 | CAW `listAuditLogs` (SSE) |
| **APIs** | **服务发现** | **Bazaar `/discovery/resources`** |

### API 路由设计

```
GET /api/services?limit=20&offset=0&type=http
GET /api/services?query=weather+data&network=eip155:8453&limit=10
```

**参数：**

| 参数 | 类型 | 默认 | 说明 |
|---|---|---|---|
| `query` | string | — | 语义搜索关键词 |
| `network` | string | — | 按链筛选，如 `eip155:8453` |
| `asset` | string | — | 按代币筛选，如 `usdc` |
| `limit` | number | 20 | 每页条数 |
| `offset` | number | 0 | 偏移量 |

**后端逻辑：**
1. 优先调用 Bazaar `/discovery/resources`（分页目录）
2. 如果有 `query` 参数，调用 `/discovery/search`（语义搜索）
3. 失败时返回 fallback 服务列表

### 右侧面板 APIs Tab 实现

```
┌─────────────────────────────────────────────────┐
│ [Tx Ledger] [Logs] [APIs]              [▲/▼/◀]  │
├─────────────────────────────────────────────────┤
│ 🔍 [搜索服务...___________________]  [搜索]     │
│                                                  │
│ ┌──────────────────────────────────────────────┐│
│ │ 🌤️  Weather Data API                         ││
│ │ GET https://api.example.com/weather          ││
│ │ $0.0001 ETH | eip155:84532                   ││
│ │ [调用]                                        ││
│ └──────────────────────────────────────────────┘│
│ ┌──────────────────────────────────────────────┐│
│ │ 📊 ETH Chain Analysis                        ││
│ │ GET /api/data/eth-analysis                   ││
│ │ 0.0001 ETH | ETH Sepolia (本地)              ││
│ │ [调用]                                        ││
│ └──────────────────────────────────────────────┘│
│ ...                                            │
│                                                  │
│ [< 1 2 3 ... 10 >]        共 200 个服务          │
└──────────────────────────────────────────────────┘
```

**功能：**
- 搜索框：输入关键词，回车搜索
- 服务卡片：名称、URL、价格、网络、调用按钮
- 分页：20 条/页，底部翻页
- 调用按钮：触发 Agent 对话调用该服务
- 来源标记：区分 Bazaar 远程服务和本地 fallback 服务

### 侧边栏 Available APIs 精简

侧边栏只展示 3 个推荐服务 + "查看全部"按钮：

```
┌──────────────────────┐
│ 🏪 Available APIs    │
│                      │
│ 🌤️ Weather Data      │
│    $0.0001 ETH       │
│                      │
│ 📊 ETH Analysis      │
│    0.0001 ETH        │
│                      │
│ [查看全部 →]          │
└──────────────────────┘
```

点击"查看全部"→ 右侧面板切换到 APIs Tab。

### 文件结构

```
app/api/services/
  route.ts              ← 已有，更新支持分页+搜索

components/panel/
  RightPanel.tsx        ← 更新：新增 APIs Tab
  ServiceList.tsx       ← 更新：精简为 3 个 + 查看全部按钮
  ServiceCard.tsx       ← 新建：单个服务卡片组件
  ServiceGrid.tsx       ← 新建：服务列表 + 搜索 + 分页

lib/
  store.ts              ← 新增：services 状态管理
```

### 实现步骤

1. 更新 `/api/services` 支持分页和搜索参数
2. 创建 `ServiceCard` 组件（单个服务卡片）
3. 创建 `ServiceGrid` 组件（搜索 + 分页 + 卡片列表）
4. 更新 `RightPanel` 新增 APIs Tab
5. 更新 `ServiceList` 为精简版（3 个 + 查看全部）
6. 更新 `page.tsx` 移除旧的 services 逻辑
